import {
    StyleSheet,
    Platform
} from 'react-native';

const styles = StyleSheet.create({
    headerTopIOS: {
        height: scaleHeight(20),
        backgroundColor: colors.black,
    },
    headerTopAndroid: {
        height: 0
    }
});